# Exim filter

testprint "AAAA"

if
    $message_body contains "This message is automatically generated by JIRA" and $message_body contains ".bot updated" 
then
	testprint "YYYY"
    unseen fail text "messaje rejected by JIRA SPAM filter"
else
	testprint "XXXX"
endif
